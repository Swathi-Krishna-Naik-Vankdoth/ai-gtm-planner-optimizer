[phases.install]
cmds = ["pip install fastapi uvicorn anthropic python-dotenv"]

[start]
cmd = "python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
```

Press **Ctrl + S**.

---

**Step 2** — Also update `Procfile` to make sure it's correct:
```
web: python -m uvicorn main:app --host 0.0.0.0 --port $PORT